# Chat Server

## Setup Instructions

1. Clone the repository.
2. Install dependencies: `npm install`
3. Start the server: `npm start`

## Endpoints

- POST `/login`
- POST `/messages`
- GET `/messages`

## Testing

- Run tests: `npm test`
